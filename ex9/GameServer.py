class GameServer:
    
    GAMES = ["numbers", "nim"]

    def __init__():

        if game in GAMES and player < players_max:
            Game = __import__(game)
            self.start_server
            
        while joined_players < expected_players:
            self.allow_join
            if 
        else:
            error = "No such game"

    

    def start_server(self)

